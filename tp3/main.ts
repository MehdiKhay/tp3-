import { add } from './math.js';
import type { user } from './type.js';

console.log( add(5, 3));
let user1: user = {
    id: 1,
    name: "alice",
    email: "alice@mail.com"
};
console.log(user1);
